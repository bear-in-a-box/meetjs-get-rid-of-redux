import React from 'react';
import ReactDOM from 'react-dom';
import { Layout } from 'components/Layout';
import './index.scss';

import * as connection from 'data/connection';

ReactDOM.render(<Layout />, document.getElementById('root'));
